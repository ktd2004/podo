1. setup SDLDIR environment
~$ export CC=arm-linux-gcc
~$ export CXX=arm-linux-g++
~$ export SDLDIR=/path/SDL-dir
~$ export SDLIMAGEDIR=/path/SDL_image-dir

2. Extract podo source code
~$ tar xzf podo-version.tgz

3. Make a shadow build directory
~$ mkdir build
~$ cd build
~/build$ 

4. Make
~/build$ cmake -DCMAKE_TOOLCHAIN_FILE=../podo-version/toolchain/XXXX.cmake ../podo-version
~/build$ make

5. Run demo
~/build$ export PD_DIR=$PWD
~/build$ cd demo/dvr
~/build$ ./dvr


